#include <iostream>

using namespace std;

int main()
{
    vector<string> values
    stack<string> s;
    int len = values.size();
    for(int i=0;i<len;i++){
        cout << values[i];
    }

    return 0;
}
